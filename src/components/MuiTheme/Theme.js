import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#34495E"
    },
    error: {
      main: "#E2383F"
    },
    secondary: {
      main: "#1ABC9C"
    }
  }
});

export default theme;
